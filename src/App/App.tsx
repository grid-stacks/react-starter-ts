import React, { FC } from "react";
import ExampleComponent from "../store/ExampleComponent";

const App: FC = () => {
	return (
		<div className="App">
			<h1>React Starter TS!</h1>
			<ExampleComponent />
		</div>
	);
};

export default App;
